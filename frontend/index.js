import React from 'react';
import ReactDOM from 'react-dom/client';
import GrooveIDApp from './GrooveIDApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<GrooveIDApp />);
