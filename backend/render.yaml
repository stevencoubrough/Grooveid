services:
  - type: web
    name: grooveid-backend
    env: python
    buildCommand: |
      apt-get update && apt-get install -y tesseract-ocr libgl1-mesa-glx
      pip install -r requirements.txt
    startCommand: uvicorn backend.groove_id_backend:app --host 0.0.0.0 --port $PORT
